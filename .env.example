# =============================================================================
# GeoRoute Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env or service-account.json to version control!
#
# Quick Start:
#   1. cp .env.example .env
#   2. Fill in GOOGLE_MAPS_API_KEY and either GEMINI_API_KEY or Vertex AI settings
#   3. docker compose up -d
#   4. Access at http://localhost (or your NGINX_PORT)
# =============================================================================

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# NGINX port (main entry point)
NGINX_PORT=80

# Backend port (optional - for direct access during development)
BACKEND_PORT=9001

# UI port (optional - for direct access during development)
UI_PORT=8080

# CORS allowed origins (comma-separated)
# For production, set to your domain
CORS_ORIGINS=http://localhost,http://localhost:80

# API URL for UI (leave empty for nginx proxy mode - recommended)
# Only set if accessing backend directly without nginx
VITE_API_URL=

# Enable hot-reload for development (set to false for production)
RELOAD=false

# =============================================================================
# GOOGLE CLOUD (REQUIRED)
# =============================================================================

# Google Cloud Project ID
# Get from: https://console.cloud.google.com
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Maps API Key
# Enable: Maps Elevation API, Maps Static API
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =============================================================================
# AI SERVICE - Choose ONE option
# =============================================================================

# -----------------------------------------------------------------------------
# OPTION 1: Vertex AI (Recommended for Production)
# -----------------------------------------------------------------------------
# Higher quotas, better reliability, requires service account
#
# Setup:
#   1. Enable Vertex AI API in Google Cloud Console
#   2. Create service account with roles/aiplatform.user
#   3. Download JSON key and save as service-account.json in project root
#
USE_VERTEX_AI=true
VERTEX_LOCATION=us-central1

# -----------------------------------------------------------------------------
# OPTION 2: AI Studio / Gemini API (Simpler setup)
# -----------------------------------------------------------------------------
# Free tier available, simpler setup, lower quotas
# Get key from: https://aistudio.google.com/app/apikey
#
# USE_VERTEX_AI=false
# GEMINI_API_KEY=your-gemini-api-key
