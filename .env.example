# GeoRoute Environment Configuration
# Copy this file to .env and fill in your values
# ALL VARIABLES ARE REQUIRED UNLESS MARKED OPTIONAL

# =============================================================================
# SERVER CONFIGURATION (REQUIRED)
# =============================================================================

# Backend server port - NO DEFAULT, must be explicitly set
BACKEND_PORT=8001

# Backend server host
BACKEND_HOST=0.0.0.0

# UI server port (for docker-compose)
UI_PORT=8080

# CORS allowed origins (comma-separated)
# Example: http://localhost:8080,http://localhost:8081
CORS_ORIGINS=http://localhost:8080

# API URL for UI to reach backend
VITE_API_URL=http://localhost:8001

# =============================================================================
# GOOGLE APIS (REQUIRED)
# =============================================================================

# Google Cloud Project ID
# Get from: https://console.cloud.google.com
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Maps API Key
# Enable: Maps Elevation API, Maps Static API, Directions API
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =============================================================================
# AI SERVICE - Choose ONE option:
# =============================================================================

# OPTION 1: AI Studio (simpler, free tier available)
# Get key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key

# OPTION 2: Vertex AI (billing required, higher quotas)
# Set USE_VERTEX_AI=true and configure service account credentials
# USE_VERTEX_AI=true
# VERTEX_LOCATION=us-central1
