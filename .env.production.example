# =============================================================================
# GeoRoute Production Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env or service-account.json to version control!

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# NGINX port (public-facing)
NGINX_PORT=80

# CORS origins (your domain)
CORS_ORIGINS=http://your-domain.com,https://your-domain.com

# API URL for frontend (use relative path with nginx proxy)
VITE_API_URL=

# =============================================================================
# GOOGLE APIS (REQUIRED)
# =============================================================================

# Google Cloud Project ID (must match service account)
GOOGLE_CLOUD_PROJECT=your-project-id

# Google Maps API Key
# Enable: Maps Elevation API, Maps Static API
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =============================================================================
# AI SERVICE - Vertex AI (Recommended for Production)
# =============================================================================

# Enable Vertex AI (recommended for production - higher quotas)
USE_VERTEX_AI=true
VERTEX_LOCATION=us-central1

# Note: Place your service-account.json file in the project root
# The file should have roles/aiplatform.user permission

# =============================================================================
# OPTIONAL: AI Studio Fallback
# =============================================================================
# Only use if not using Vertex AI
# GEMINI_API_KEY=your-gemini-api-key
